<UserControl x:Class="KafkaToolWpf.Views.CreateTopicDialogWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             Width="420" Height="380">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="ResizeMode" Value="NoResize"/>
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="SizeToContent" Value="WidthAndHeight"/>
            <Setter Property="Title" Value="创建Topic"/>
        </Style>
    </prism:Dialog.WindowStyle>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Topic名称：" FontSize="14" Margin="0 5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding TopicName}" FontSize="14" Height="28" Margin="0 5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="分区数：" FontSize="14" Margin="0 5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PartitionCount}" FontSize="14" Height="28" Margin="0 5"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="副本因子：" FontSize="14" Margin="0 5" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding ReplicationFactor}" FontSize="14" Height="28" Margin="0 5"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="高级配置：" FontSize="14" Margin="0 5" VerticalAlignment="Top"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding ConfigEntries}" FontSize="12" Height="80"
                 AcceptsReturn="True" TextWrapping="Wrap" Margin="0 5"
                 ToolTip="每行一个配置项，格式: key=value&#x0a;例如: cleanup.policy=compact&#x0a;retention.ms=604800000"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding ErrorMessage}" 
                   Foreground="Red" FontSize="12" Margin="0 5"/>

        <StackPanel Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0 10">
            <Button Command="{Binding CreateCommand}" Content="创建" FontSize="14" Width="80" Height="30" Margin="0 0 10 0"/>
            <Button Command="{Binding CancelCommand}" Content="取消" FontSize="14" Width="80" Height="30"/>
        </StackPanel>
    </Grid>
</UserControl>